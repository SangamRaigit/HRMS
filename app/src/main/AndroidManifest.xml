<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.management.system">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.hrms">
        <activity android:name=".AllApplicationsActivity"></activity>
        <activity android:name=".AllUsersActivity" />
        <activity android:name=".JobsActivity" />
        <activity android:name=".PostJobActivity" />
        <activity android:name=".ProfileScreenActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".EmployeesHomeActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".JobseekerHomeActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".JobRolePredictionActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>